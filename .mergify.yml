merge_protections_settings:
  reporting_method: deployments

pull_request_rules:
  - name: VM4 WA merge must honor deployment-reporting Merge Protections
    conditions:
      - title = VM4_WA_MP_DEPLOYMENT_ENFORCEMENT_20260917
    actions:
      merge:
        method: merge
