merge_queue:
  mode: serial

queue_rules:
  - name: VM4 Batch Peek capture
    batch_size: 1
    merge_method: merge
    queue_conditions:
      - base = main
    merge_conditions:
      - check-success = vm4-batch-peek-guard
