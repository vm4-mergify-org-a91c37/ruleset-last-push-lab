pull_request_rules:
  - name: VM4 same-SHA cross-PR WA direct merge
    conditions:
      - title = VM4_WA_SAME_SHA_VICTIM_20260917
      - check-success = @github-actions/vm4-wa-same-sha-gate
    actions:
      merge:
        method: merge
