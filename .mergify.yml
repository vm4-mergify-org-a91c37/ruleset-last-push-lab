merge_queue:
  mode: serial
queue_rules:
  - name: VM4 close-reopen manual queue generation
    batch_size: 1
    merge_method: merge
    queue_conditions:
      - base = vm4-reopen-base-20260917
    merge_conditions:
      - check-success = VM4_REOPEN_FINAL_HOLD_NEVER
